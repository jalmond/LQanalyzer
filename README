//###########################
//   Author John Almond
//###########################
Steps to run analysis:
1) Download LQanalyzer:
git clone git@github.com:jalmond/LQanalyzer.git
cd LQanalyzer/
git checkout -b LQanalyzer_Dec13 origin/LQanalyzer_Dec13
 
2) in LQanalyzer main directory type:
source setup.sh

## NOTE
 - If running on SNU machines this should work out of box. 
 - If running on any other machine you need to setup root enviroment before hand/ and maybe add your root_setup script to the setup.sh file (you may also need to modify Makefile.common, if your Makefile.arch script is not found) 


### TEST CYCLE  ###

To run the test cycle

1) cd $LQANALYZER_RUN_PATH
2) source submit.sh ( you can modify the number of cores in this or change the amount of data if you like)


### Make cycle ###
From $LQANALYZER_CYCLE_DIR you can use command:
LQanalyzer_create_cycle.py <cyclename>

this will create a skeleton cycle named cyclename:
a) in $LQANALYZER_SRC_PATH it will create cyclename.cc. You can then modify this accordingly.
b) in $LQANALYZER_INCLUDE_PATH it will create cyclename.h.
c) it will add cyclename to LinkDef file.



